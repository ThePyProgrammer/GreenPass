<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thepyprogrammer.greenpass">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenPass">
        <activity
            android:name=".ui.loginRegister.LoginRegisterActivity"
            android:label="@string/title_activity_login_register"
            android:theme="@style/Theme.GreenPass.LoginRegister">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.GreenPass.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.list.ItemListActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GreenPass.DarkActionBar" />
        <activity
            android:name=".ui.detail.ItemDetailActivity"
            android:label="@string/title_item_detail"
            android:parentActivityName=".ui.list.ItemListActivity"
            android:theme="@style/Theme.GreenPass.DarkActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.thepyprogrammer.greenpass.ui.list.ItemListActivity" />
        </activity>
    </application>

</manifest>